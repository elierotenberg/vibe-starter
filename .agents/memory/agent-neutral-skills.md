# Agent-neutral skills

Shared project skills live under `.agents/skills/`, one directory per skill with `SKILL.md` as the entry point. Keep their core instructions portable across agent products.

Per-tool skill locations are links to the shared tree. Do not duplicate skill content under `.claude`, `.codex`, or another tool-specific directory. Add tool-specific metadata only when it is optional and does not change the shared workflow.
