# Never disable checks

Never suppress or weaken a type check, lint rule, warning, test, CI check, or security control to make work pass without explicit user approval. This includes ignore directives, disabled rules, skipped failing tests, relaxed thresholds, and permissive continuation settings.

Fix the cause. If the check is wrong, unavailable, or outside the task's authority to repair, show the evidence and ask before changing the check itself.
